{
  "name": "aegis-robotics-platform",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "concurrently -k -n server,web -c blue,cyan \"pnpm --filter server dev\" \"pnpm --filter web dev\"",
    "build": "pnpm --filter web build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
