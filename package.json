{
  "name": "aegis-robotics-platform",
  "private": true,
  "version": "2.4.0",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "postinstall": "pnpm --filter @plantbot/adapter-sdk build",
    "dev": "concurrently -k -n server,web,robots,relay -c blue,cyan,magenta,yellow \"PB_DEMO=1 PB_DEV_KEYS=1 SESSION_SECRET=plantbot-dev-only MEDIA_RELAY=http://127.0.0.1:1984 pnpm --filter server dev\" \"pnpm --filter web dev\" \"pnpm --filter integrations dev\" \"node scripts/relay.mjs\"",
    "dev:core": "concurrently -k -n server,web -c blue,cyan \"PB_DEMO=1 PB_DEV_KEYS=1 SESSION_SECRET=plantbot-dev-only pnpm --filter server dev\" \"pnpm --filter web dev\"",
    "build": "pnpm --filter web build",
    "release:build": "node scripts/build-release.mjs",
    "adapter": "pnpm --dir integrations exec tsx runtime.ts",
    "test:vision": "integrations/vision/.venv/bin/python -m unittest discover -s integrations/vision/tests -v"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "concurrently": "^10.0.3",
    "playwright": "^1.61.1"
  }
}
